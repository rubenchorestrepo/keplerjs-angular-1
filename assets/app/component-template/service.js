import {Factory} from 'config/factory';

export class [componentName]Service extends Factory
{

    config(httpFactory)
    {
        "ngInject";
         //Define your methods here
         var factory = {};
         return factory;
    }
}