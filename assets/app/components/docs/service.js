import {Factory} from 'config/factory';

export class docsService extends Factory
{

    config(httpFactory)
    {
        "ngInject";
         //Define your methods here
         var factory = {};
         return factory;
    }
}